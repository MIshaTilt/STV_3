| № | Раздел | Требования к заполнению |
|---|---|---|
| 1 | **Test Plan Identifier** | TP-TAB-2024-03 |
| 2 | **Introduction** | Тестирование модуля отображения каталога столов. Проверка доступности маршрутов, корректности статусов и рендеринга шаблонов/контекста в рамках 3-й части курса "Сборка, тестирование и верификация". |
| 3 | **Test Items** | `TableListView` (Список), `TableDetailView` (Детальное описание), `AboutView` (О проекте). |
| 4 | **Features to be tested** | Вывод списка, фильтрация по материалу (`?type=...`), сортировка, детальный просмотр товара, статическая страница about, ошибка 404 для несуществующих товаров. |
| 5 | **Features NOT to be tested** | Авторизация и права доступа, формы создания/редактирования, POST-запросы, изменение данных, API. |
| 6 | **Approach** | Использование `django.test.Client`, выполнение исключительно GET-запросов, проверка валидации контекста (наличие нужных ключей) и ответа (status_code). |
| 7 | **Item Pass/Fail Criteria** | Успех: Status 200/404 соответствуют ожиданиям, загружены верные шаблоны, контекст содержит данные. |
| 8 | **Suspension Criteria** | >30% тестов упали, фатальные ошибки взаимодействия с БД во время инициализации `setUpTestData`. |
| 9 | **Test Deliverables** | Исходный код тестов в пакете `tests/` (`test_routes.py`, `test_content.py`), отчет о запуске `--verbosity=2`, coverage report. |